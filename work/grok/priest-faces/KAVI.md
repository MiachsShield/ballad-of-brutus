# Kavi — what I need from you (Grok, 2026-09-16)

G1 is in progress on `grok/priest-faces`. I cannot put JPEGs on the branch:
the GitHub connector only writes UTF-8 text. That is the whole delivery
blocker.

## Please do these three things

1. **Binary path.** Either pull the 11 wave-1 JPEGs from this Grok chat /
   the session zip and drop them at
   `art/card-faces/priest/<id>.jpg` on this branch, or give the connector
   a binary-safe upload. Exact ids, no renaming.

2. **One costume lock before I draw the other 27.** Wave 1 uses bone-cloth
   vestment + soot-violet stole + brass sun-disc. Confirm that as the
   Priest combat-card uniform, *or* tell me every face must be shaved-buzz
   Brutus. Do not let me draw 27 more in the wrong body.

3. **K8 wiring can start on these 11.** You do not need all 38 to prove
   the hand shows a face. Highest-value first wire:
   `priest_candle_prayer`, `priest_radiant_glare`, `priest_blazewhirl`,
   `priest_bulwark_slam` (cheap / medium / AoE / max-cost).

## Also waiting

- One-second read on Bear Hug Break pass 2 (`OXuck.jpg` in the earlier
  session). G3 is blocked on that.
- Combat-card pixel box size, if you have it, so later crops match the
  live hand.

## I will not

- Touch the HTML build.
- Invent costs or rules (catalog is from the shipped file).
- Merge this PR.

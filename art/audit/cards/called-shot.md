# Called Shot

- **Name:** Called Shot
- **Class:** Ranger — sources: `brutus-card-art-inventory.md` (Ranger wave 2
  list); `brutus-class-cards-balance-part2.md` ("## Ranger (38)" table row);
  face file in `files/brutus-card-downloads/ranger/`.
- **Energy cost:** **Not present in build or repo.** The name has zero hits
  in the shipped build (`brutus-1_0_a0mk.html`, 2026-09-12).
- **Rules text:** **Not present in build or repo.** The only numbers anywhere
  are Claude's audit-design row in `brutus-class-cards-balance-part2.md`
  (reversible balance-pass assumption, NOT build data and NOT canon):
  `| Called Shot | light | 3 | 6+cripple | A precise called shot to the leg
  (CRITICAL redo pending, see [truncated in source view]) |  |`
  — light tier, cost 3, 6+cripple, effect "A precise called shot to the leg"
  (Deck Copies cell empty in the source). Treat as design, not canon.
- **Readability failure (146-face audit):** "CRITICAL/ship-blocking: baseball
  pitcher, wrong-universe modern sport imagery" (recorded in
  `briefs/redo-queue-146.md`); the audit flags also note "Called Shot —
  critical archetype break, name/effect likely fine, art is the problem"
  (`brutus-class-cards-balance-part2.md`).
- **Current face:** `../faces/called-shot.jpg` (copied from
  `files/brutus-card-downloads/ranger/ranger-called-shot.jpg`).

**Brief-writer note:** the card's mechanics exist only as Claude's audit
design, which the audit itself flags as provisional ("name/effect likely
fine" is an assumption, and the Deck Copies cell is empty). Do not present
these numbers as canon; if the redo brief needs locked mechanics, flag it
for Robert.
